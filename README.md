# 141st JTOC — Documentation

This repository contains the sources generating the documentation for the squadron.

## Usage

Requirements:

- Python 3.12 or above
- Typst

# Generate PDFs

Run the `make.py` command. Output will be in `out`.
